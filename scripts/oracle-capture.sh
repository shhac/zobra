#!/usr/bin/env bash
# Capture oracle outputs into test/fixtures/ for differential testing.
#
# Phase 0: this is a placeholder. Phase 1+ will iterate over
# test/differential/matrix.zig (which doesn't exist yet) and produce
# test/fixtures/oracle.json with stdout/stderr/exit-code per case.
set -euo pipefail
echo "oracle-capture: not yet implemented (Phase 1)" >&2
exit 1
